### How to Install Yarn

```markdown
# How to Install Yarn 📦

**Requirements:**
- Node.js and npm installed previously.

- **Install Yarn:**
  - Run:
    ```bash
    npm install --global yarn
    ```

- **Verify Yarn:**
  ```bash
  yarn --version
